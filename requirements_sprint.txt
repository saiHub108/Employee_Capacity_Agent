# Sprint Capacity Agent — Python dependencies
# Install with: pip install -r sprint_data/requirements_sprint.txt
#
# Python 3.10+ required (google-adk requires 3.10 or later)

# ── Agent framework ────────────────────────────────────────────────────────────
google-adk>=1.0.0          # Google Agent Development Kit: Agent, SequentialAgent, Runner, McpToolset

# ── MCP (Model Context Protocol) ──────────────────────────────────────────────
mcp>=1.0.0                 # MCP Python SDK: Server, stdio_server, StdioServerParameters, Tool

# ── LLM provider (via google-adk) ─────────────────────────────────────────────
google-genai>=1.0.0        # Google GenAI client: types.Content, types.Part (used in runner.run_async)

# ── Data layer ─────────────────────────────────────────────────────────────────
pandas>=2.0.0              # DataFrame operations and Excel I/O
openpyxl>=3.1.0            # Excel engine for pandas read_excel / ExcelWriter

# ── Presentation ──────────────────────────────────────────────────────────────
tabulate>=0.9.0            # ASCII table rendering (used in capacity report)
reportlab>=4.0.0           # PDF generation with AcroForm editable fields
